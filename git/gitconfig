[user]
  email = alexander@bitraf.no
  name = Alexander Alemayhu
  signingkey = 756753E5
        
[core]
	editor = vim
[alias]
	ci = commit
	st = status
        br = branch
        ch = checkout
        clf = clean -fd
        d = rm
        l = log --oneline
        pl = pull --rebase
        ss = status -s
        forget = rm --cached
	unstage = reset HEAD --

[color]
        branch = auto
        diff = auto
        status = auto

[color "branch"]
        current = yellow reverse
        local = yellow
        remote = green

[color "diff"]
        meta = yellow bold
        frag = magenta bold
        old = red
        new = cyan

[color "status"]
        added = yellow
        changed = green
        untracked = cyan
[push]
	default = matching
[diff]
        tool = icdiff

[difftool "icdiff"]
        cmd = icdiff $LOCAL $REMOTE
